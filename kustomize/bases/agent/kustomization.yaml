resources:
  - daemonset.yaml

configMapGenerator:
- name: observe-agent-config
  files:
    - telegraf.conf
    - fluent-bit.conf
    - parsers.conf

images:
- name: telegraf
  newTag: '1.20'
- name: fluent/fluent-bit
  newTag: '1.8.7'
